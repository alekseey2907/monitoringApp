# Система предиктивного мониторинга двигателя

Приложение для мониторинга температуры и вибрации двигателя с использованием ESP32, MPU6050 и DS18B20.

## 🚀 Технологии

- **Frontend**: HTML, CSS, JavaScript, Chart.js
- **Backend**: Node.js, Express, Socket.IO
- **Desktop**: Electron.js
- **Датчики**: ESP32, MPU6050 (акселерометр/гироскоп), DS18B20 (температура)

## 📋 Возможности

- ✅ Мониторинг температуры двигателя в реальном времени
- ✅ Мониторинг вибрации (ускорение и угловая скорость)
- ✅ Визуализация данных на графиках
- ✅ Система предупреждений и алертов
- ✅ Автоматическое обнаружение ESP32 в сети
- ✅ Desktop приложение (Electron)
- ✅ Web-версия для деплоя на хостинге
- 🔄 Легко адаптируется под Bluetooth

## 🔧 Установка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка ESP32

Загрузите код ESP32 на устройство. Убедитесь, что:
- ESP32 подключен к той же Wi-Fi сети
- Настроены правильные SSID и пароль

## 🖥️ Запуск Desktop приложения

```bash
npm start
```

Или в режиме разработки (с DevTools):

```bash
npm run dev
```

## 🌐 Запуск веб-версии

Для деплоя на сервере используйте веб-версию:

```bash
npm run start-web
```

Приложение будет доступно на `http://localhost:3000`

## 🚀 Деплой на хостинг

### Вариант 1: Railway / Render / Heroku

1. Создайте `Procfile`:
```
web: node server-standalone.js
```

2. Убедитесь, что в `package.json` есть:
```json
"engines": {
  "node": ">=18.0.0"
}
```

3. Деплой:
   - **Railway**: Подключите GitHub репозиторий
   - **Render**: Создайте Web Service, команда старта: `node server-standalone.js`
   - **Heroku**: `git push heroku main`

### Вариант 2: VPS (Ubuntu)

```bash
# Клонируйте репозиторий
git clone https://github.com/alekseey2907/monitoringApp.git
cd monitoringApp

# Установите зависимости
npm install --production

# Установите PM2
npm install -g pm2

# Запустите приложение
pm2 start server-standalone.js --name "monitoring"
pm2 save
pm2 startup

# Настройте Nginx (опционально)
```

### Вариант 3: Docker

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --production

COPY . .

EXPOSE 3000

CMD ["node", "server-standalone.js"]
```

Запуск:
```bash
docker build -t monitoring-app .
docker run -p 3000:3000 monitoring-app
```

## 📊 Архитектура

```
ESP32 (датчики) 
    ↓ (Wi-Fi/TCP)
Node.js сервер (server/server.js)
    ↓ (WebSocket)
Frontend (HTML/CSS/JS)
    ↓
Electron (Desktop приложение)
```

### Поток данных:

1. **ESP32** → отправляет UDP broadcast для автообнаружения
2. **Сервер** → подключается к ESP32 по TCP (порт 12345)
3. **ESP32** → отправляет JSON данные каждую секунду
4. **Сервер** → парсит данные и отправляет через WebSocket
5. **Frontend** → получает данные и визуализирует

## 🔌 Адаптация под Bluetooth

Для перехода на Bluetooth нужно:

1. Установить пакет для работы с Bluetooth:
```powershell
npm install @abandonware/noble
```

2. Изменить код в `server/server.js`:
   - Заменить TCP подключение на Bluetooth
   - Использовать BLE (Bluetooth Low Energy)

3. На ESP32 использовать BLE вместо Wi-Fi

## ⚙️ Настройка пороговых значений

В файле `frontend/app.js` можно изменить пороговые значения для алертов:

```javascript
const THRESHOLDS = {
  tempWarning: 60,    // °C - предупреждение
  tempDanger: 80,     // °C - опасно
  accelWarning: 15,   // м/с² - предупреждение
  accelDanger: 25,    // м/с² - опасно
  gyroWarning: 3,     // рад/с - предупреждение
  gyroDanger: 5       // рад/с - опасно
};
```

## 📁 Структура проекта

```
monApp/
├── main.js                 # Главный файл Electron
├── preload.js             # Preload скрипт для безопасности
├── package.json           # Зависимости проекта
├── README.md              # Документация
├── server/
│   └── server.js          # Backend сервер (TCP, UDP, WebSocket)
└── frontend/
    ├── index.html         # Интерфейс приложения
    ├── styles.css         # Стили
    └── app.js             # Логика frontend + графики
```

## 🛠️ Разработка

### Добавление новых функций:

1. **Новые датчики**: добавьте обработку в `server/server.js`
2. **Новые графики**: добавьте в `frontend/app.js` (функция `initCharts`)
3. **Новые алерты**: настройте в `THRESHOLDS` и добавьте логику в `updateCurrentReadings`

### Отладка:

- WebSocket: проверьте консоль браузера (F12)
- Сервер: логи в терминале
- ESP32: Serial Monitor в Arduino IDE

## 📝 Примечания

- Приложение автоматически переподключается к ESP32 при разрыве соединения
- Храним последние 100 точек данных в истории
- На графиках отображаются последние 50 точек
- Максимум 10 алертов в списке предупреждений

## 🔜 Будущие улучшения

- [ ] Адаптация под Bluetooth
- [ ] Сохранение данных в БД (SQLite/PostgreSQL)
- [ ] Экспорт данных в CSV/Excel
- [ ] Machine Learning для предсказания поломок
- [ ] Уведомления на email/Telegram
- [ ] Поддержка нескольких ESP32 устройств

## 📄 Лицензия

ISC

---

**Автор**: Ваше имя
**Дата**: 18.11.2025
